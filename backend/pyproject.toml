[project]
name = "sheet-to-neon-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "psycopg2-binary",
    "pgvector",
    "voyageai",
]

[tool.vercel]
entrypoint = "api.sync:handler"
